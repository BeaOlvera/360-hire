# Log — 360 Hire

<!-- APPEND ONLY. Never edit existing entries. Newest entries at the bottom. -->

### 2026-05-24 — checkpoint: Big Five + ICAR + Resilience assessments, deeper competency-based interview prompt, periodic video snapshot, CV/video moved to Supabase Storage, Phase 6.6 convert-to-job-fit, Comprehensive Report
Eight substantive changes in this stretch.
1) Storage migration off Vercel Blob to Supabase Storage. Created cv (15 MB) and video (50 MB) public buckets. Rewrote /api/apply/[token]/cv and /video to use supabaseAdmin.storage. CV upload tested end-to-end with a real PDF: 200 OK, Anthropic text extraction returns the CV text. No more "CV storage not configured" error.
2) OpenAI key copied from climate_app/.env.local into 360_hire/.env.local. Voice input transcribe endpoint smoke-tested with synthetic silent WAV; returns 200 with Whisper text. Voice toggle gracefully degrades with a clear message if the server has no OPENAI_API_KEY.
3) Periodic video snapshot upload: bitrate 250 kbps + 480x360 @ 12 fps; MediaRecorder timeslice 15s, accumulated chunks re-uploaded every 60s to a stable Supabase Storage path so partial recordings survive tab close. Admin review page surfaces a "Partial - candidate did not finish" pill when video exists but status is not completed; URL cache-busted on each page load.
4) Per-job competencies: migration 005 adds jobs.competencies JSONB. NewJobForm has a competencies editor (name + Critical / Important / Relevant). JD-file-upload extraction auto-fills suggested competencies. score_fit + the deep interview prompt now consume these.
5) Deeper interview prompt: ported the 5-phase Critical Incident structure from 360_app's prompts_self.ts (rapport / frame / past / present competency deep-dive / future + motivation), competency-weighted, anti-fabrication safeguards (sensory anchoring, temporal specificity, detail probing, cross-referencing, copy-paste/AI detection, failure probing, consistency monitoring). EN+ES with full accents. New buildGenericPrompt for no-job evaluations.
6) Migration 006 extends the assessment_responses CHECK to allow three new codes: big_five, icar_reasoning, resilience. Three new assessment libs added: big_five (30 IPIP items public domain, 6 per factor E A C N O, reverse-scored, per-trait bars), icar_reasoning (12 multiple-choice items in ICAR style, original constructions, 4 subdomains verbal/number/letter/logic, banded result + subdomain breakdown), resilience (6 Likert items composed in the BRS framework, freshly written, reverse-scored, banded mean). Registry updated (types.ts AssessmentCode union, index.ts ASSESSMENTS map, NewJobForm ASSESSMENT_OPTIONS list, /api/admin/jobs ALLOWED_ASSESSMENTS, /api/apply/[token]/assessment isGeneric defaults). All three follow the existing Assessment.tsx single-page-submit UI pattern.
7) Phase 6.6 - Convert generic to job fit: /api/admin/applications/[id]/score now accepts {job_id} in the body; when the application has no job_id, it assigns the chosen job and runs scoring against that JD. ScoringPanel shows a job-dropdown when the eval is generic (instead of the plain Generate fit score button); click Convert to job fit -> server assigns + scores in one POST.
8) Comprehensive Report: new lib/generate_comprehensive_report.ts. synthesizeCandidate runs ONE claude-sonnet-4-6 call passing JD + per-job competencies + CV + full transcript + existing score_data + all completed complementary assessment scores. Returns structured ComprehensiveResult (executive_summary, recommendation, recommendation_rationale, top_strengths, top_concerns, risk_flags, competencies array with per-competency interview_evidence + interview_gaps + assessment_signals + rating, cross_signal_observations, next_steps). generateComprehensiveHTML renders to a printable single page with executive summary + recommendation on top, then strengths/concerns, competencies coded, cross-signal observations, assessments at-a-glance, suggested next steps, transcript appendix. New endpoint /api/admin/applications/[id]/comprehensive serves it. ScoringPanel adds a Comprehensive report button next to the fit-report links. Audit action ai.comprehensive_report logged on each generation. EN+ES.
User must run migrations 005 + 006 in Supabase. tsc + next build green; 360_app working tree confirmed clean.

### 2026-05-24 — Phase 6 (almost) complete: nullable jobs, Candidates section, JD upload, generic eval, Culture Fit (OCAI), Overview gate, monochrome palette, consent + saving bugs fixed
Round 2 of consent debugging: dev-server log showed POST /consent returning 200 fine and direct DB query confirmed the privacy_consents row was being inserted correctly, AND the server-side hasConsent check returned true. So the bug was NOT the insert. Root cause: the candidate's browser was holding a stale client bundle that kept showing PrivacyGate. Defenses added: `export const dynamic = 'force-dynamic'` + `revalidate = 0` + `fetchCache = 'force-no-store'` on app/apply/[token]/page.tsx so the server never serves a cached route segment. Also: Assessment.tsx + CultureFitAssessment.tsx were stuck on "Saving..." because they used `router.refresh()` which didn't reliably re-render Next 14 server components — switched both to `window.location.reload()` matching PrivacyGate. Wrote diagnose_consent.mjs + simulate_apply_page.mjs scripts under scripts/ (Node + supabase-js) for future direct-DB diagnosis.

Then executed Phase 6 in one continuous block:
- (6.1) Migration 004_phase6.sql: ALTER applications.job_id DROP NOT NULL (generic evals), ALTER jobs ADD culture_profile JSONB, extended assessment_responses CHECK to include 'culture_fit'.
- (6.2) JD file upload: new POST /api/admin/jobs/extract-from-file using Anthropic haiku-4-5 + document content type to extract {title, description, org_level, language, suggested_competencies} from a PDF JD. New "Upload JD" button on NewJobForm auto-fills; admin reviews before saving. New ai.jd_extraction audit action.
- (6.3) Candidates section: /admin/candidates list, /admin/candidates/new form, /admin/candidates/[id] detail with all evaluations + actions panel ("Assign to job" picks from open jobs dropdown; "Start generic evaluation" creates application with job_id=NULL). New endpoints POST /api/admin/candidates and POST /api/admin/candidates/[id]/evaluate. AdminHeader gets a "Candidates" link; middleware extended.
- (6.4) Generic evaluation flow: applications can be created with job_id=NULL; chat route picks buildGenericPrompt() (new function in lib/prompts.ts; career-discovery / developmental, EN+ES with full accents) instead of buildCandidatePrompt() when no job; the assessment endpoint defaults to enabling all 3 standard assessments for generic evals; jobTitle in candidate UI becomes "general evaluation" / "evaluación general".
- (6.5) Culture Fit (OCAI): new lib/assessments/culture_fit.ts with 6 OCAI dimensions × 4 quadrants (Clan/Adhocracy/Market/Hierarchy), 100-point allocation per dimension. cultureFitDistance() returns 0..1 fit vs company profile via normalized Euclidean distance. New CultureFitAssessment.tsx candidate UI (range slider + numeric input per option, must total 100 per dimension). Per-job culture profile editor on NewJobForm with 4 sliders summing to 100, saved to jobs.culture_profile. HTML report includes an SVG radar overlay (candidate vs company) + 4-quadrant breakdown.

Mid-session user complaints handled:
- (A) "Saving..." stuck on Growth Orientation: fixed (router.refresh -> window.location.reload).
- (B) Need an overview page before tests: built Overview.tsx + POST /api/apply/[token]/overview-seen. Gate flow is now consent -> CV -> OVERVIEW -> assessments -> interview. Overview lists numbered steps with estimated minutes and short explanation.
- (C) No green colors in assessment results: converted all 4 assessment palettes to a 4-shade neutral grey palette (#0A0A0A / #3F3F3F / #6B6B6B / #AEABA3) with subtle backgrounds (#E2E0DA / #EAEAEA / #F0EEE8 / #F5F4F0). Updates: Thinking Style quadrants, Growth Orientation level colors + ring gauge, Career Values 8 anchors, Culture Fit 4 quadrants + radar polygons + legend. Status pills (Reviewed/Hired/Rejected) kept their semantic colors.

NOT YET DONE in Phase 6 (deferred to next session): "Convert generic eval to job fit" admin action. Skeleton ready, just needs UI on the application review page + a small endpoint that runs lib/score_fit.ts on the existing transcript against a chosen JD.

User must apply migration 004_phase6.sql in Supabase before creating jobs with culture_fit enabled or starting generic evals. tsc + next build green; 33 routes total. 360_app working tree confirmed clean (only CONTEXT.md / LOG.md modifications from the earlier /ctx-save).

### 2026-05-24 — consent bug fix + Spanish accents sweep + Phase 6 plan
User live-tested the candidate flow and reported (1) clicking "accept all clauses" in PrivacyGate didn't advance to CV upload, and (2) all the Spanish UI text was written without accents. Dev-server log confirmed the bug: POST /api/apply/[token]/consent was returning 200 multiple times in a row but the page kept showing PrivacyGate after refresh, i.e. the insert was silently failing. ROOT CAUSE: the consent route did `await supabaseAdmin.from('privacy_consents').insert(...)` without destructuring or checking the `error` field, so any DB-side failure (RLS, schema mismatch, constraint) would return 200 to the client. FIX: rewrote the route to capture `error: insertError`, return 500 with the actual message on failure, log to console.error, and made the route idempotent (returns 200 with already_recorded:true if a consent row already exists). Also switched PrivacyGate from router.refresh() to window.location.reload() since the previous approach wasn't reliably re-rendering the server component after the POST. Accents sweep: fixed PrivacyGate, CVUpload, InterviewChat, Assessment, app/api/apply/[token]/chat/route.ts (Spanish moderation message), lib/email.ts (bodyEs), lib/prompts.ts (SHARED_INSTRUCTIONS_ES + buildCandidatePrompt ES branch), lib/score_fit.ts (SYSTEM_PROMPT_ES), lib/generate_report.ts (TEXTS.es), lib/assessments/thinking_style.ts (LONG_ES + intro), lib/assessments/growth_orientation.ts (INTERPRETATION_ES + method note), lib/assessments/career_values.ts (method note). Saved a user-level feedback memory at C:\Users\bolve\.claude\projects\C--Users-bolve\memory\feedback_spanish_accents.md so future sessions don't make the same mistake. Phase 6 PLANNED (not built): nullable applications.job_id for generic evaluations; new /admin/candidates section (list + create + detail + assign-to-job / start-generic); JD file upload with Anthropic extraction; generic evaluation flow with a career-discovery prompt and a "convert to job fit" action; Culture Fit assessment using OCAI (Cameron & Quinn 1999) with per-job culture profile + radar-overlay visualization. Recommended OCAI over OCP / Goffee-Jones / O*NET / Cable-DeRue for v1. Effort ~10-11h. tsc clean. 360_app working tree unchanged.

### 2026-05-23 — Phase 5 complete: three complementary assessments (Thinking Style / Growth Orientation / Career Values), EN+ES
Built three optional per-job assessments wired into the candidate flow between CV and interview. New migration supabase/migrations/003_add_assessments.sql adds a jobs.assessments JSONB column (default '[]') and an assessment_responses table (application_id x assessment_code unique, raw_answers JSONB, scores JSONB, RLS service-role-only). New lib/assessments/ module: types.ts, thinking_style.ts (12 forced-choice items mapping to AZUL/VERDE/AMARILLO/ROJO quadrants per the user-provided Herrmann simplification with full quadrant interpretations EN+ES from notes/2026-05-23-herrmann-quadrants-source.md), growth_orientation.ts (18 LLAS Likert items, 3 subscales DL/SF/DS, EN+ES translated, CC BY citation), career_values.ts (24 Likert items, 8 Schein-mapped anchors TF/GM/AU/SE/EC/SV/CH/LS, original wording, EN+ES). Each lib exposes score(raw) and generateHtml(args) — Thinking Style produces a 2x2 colored quadrant grid with the verdict + strengths/watchouts, Growth Orientation produces an SVG ring gauge + horizontal subscale bars, Career Values produces top-3 cards + a vertical bar chart of all 8 anchors with the top 3 highlighted. Each test generates a standalone printable A4-style HTML report with visualization and personalized interpretation, branded teal. New endpoint POST /api/apply/[token]/assessment validates code/answers, calls score(), upserts. New endpoint GET /api/admin/applications/[id]/assessment/[code] renders the HTML report for admin view. Candidate UI: new Assessment.tsx single-page form with progress bar, localStorage autosave per-token+code, Likert 5-point buttons and lettered option cards for forced-choice. Gate routing in app/apply/[token]/page.tsx now goes consent -> CV -> all enabled assessments in registry order -> interview. Admin UI: NewJobForm adds 3 checkbox cards; POST /api/admin/jobs validates and persists; the application review page adds a "Complementary assessments" card showing status per enabled test (Pending / Completed with one-line summary + Open report link). lib/audit.ts adds 'assessment.completed' action. 26 routes total. Migration 003 must be applied before this works. tsc + next build green; 360_app working tree confirmed clean. Did NOT yet feed assessment scores into lib/score_fit.ts (the fit scoring prompt) — that's a follow-up; assessments are currently independent of the AI recommendation.

### 2026-05-23 — checkpoint
360 Hire was created from scratch this session as a sibling app to 360 Evaluate. Walked Phases 0 through 4.1 in one pass: scaffold + memory + migrations + lib copies (Phase 0), admin auth + jobs CRUD + candidate invite (Phase 1), candidate flow with privacy + CV upload (Anthropic PDF extraction) + interview chat with voice I/O ported from 360_app (Phase 2), continuous video recording + admin review screen with video player + transcript (Phase 3), AI fit scoring + hiring report HTML + decision controls (Phase 4), then PDF export via puppeteer-core + @sparticuz/chromium and override-regenerates-report via a new score_data JSONB column (Phase 4.1). Build green throughout, 23 routes total, 360_app working tree confirmed clean after every phase. The product is v1 feature-complete locally; not yet deployed and not yet tested live end-to-end. Open: migration 002 must be applied, CHROME_EXECUTABLE_PATH must be set for local PDF, end-to-end smoke test, Vercel deploy.

### 2026-05-23 — Phase 4.1: PDF export + manual-override regenerates the report
Two follow-ups to Phase 4 that the user requested. (1) Persist the structured FitResult so manual overrides can regenerate the rendered report: added supabase/migrations/002_add_score_data.sql adding a JSONB score_data column on applications; the POST /score route now saves score_data alongside report_html and fit_score and recommendation; the PATCH /recommendation route now, when the recommendation field actually changes, loads score_data, patches the recommendation in-place, re-renders report_html via generate_report.ts with the same candidate+job+language context, and persists both the patched score_data and the new report_html. The audit log entry now carries report_regenerated:true for visibility. (2) Real downloadable PDF: installed puppeteer-core@^25 + @sparticuz/chromium@^148, new helper lib/pdf.ts that picks the chromium binary based on environment (Vercel/Linux uses @sparticuz/chromium's bundled binary; local dev reads CHROME_EXECUTABLE_PATH env var pointing to a system Chrome/Edge/Brave), new endpoint GET /api/admin/applications/[id]/report.pdf that loads the saved report_html and runs puppeteer's page.pdf({format:'A4', printBackground:true, margin: 14/12/16/12 mm}), returns application/pdf with a Content-Disposition filename '360hire_<jobSlug>_<candidateSlug>.pdf'. Added experimental.serverComponentsExternalPackages: ['puppeteer-core','@sparticuz/chromium'] in next.config.js so Webpack doesn't try to bundle the chromium binary. Added a "Download PDF" button (black) on the ScoringPanel next to "View full report". Updated .env.example with a CHROME_EXECUTABLE_PATH section. Migration 002 must be applied in Supabase before this works; otherwise the score endpoint will throw on the score_data column insert. tsc + next build both green; 23 routes total. 360_app working tree confirmed clean.

### 2026-05-23 — Phase 4 complete: AI fit scoring + hiring report + admin decision controls
Built the scoring + recommendation step. lib/score_fit.ts runs one claude-sonnet-4-6 call passing JD + CV text + full transcript and returns a structured FitResult: 4-7 extracted-from-JD competencies (each with score 0-5, weight 0-1, evidence quotes, gaps), overall_fit (0-1, recomputed defensively from weighted average if the model's number diverges), recommendation enum (strong_hire / hire / maybe / no_hire) with default thresholds at 0.80 / 0.65 / 0.45, plus one_line_summary, strengths, concerns, risk_flags and next_steps. EN + ES prompt variants; JSON-only output with defensive parsing (regex-extract, type coerce, weight re-normalise if not summing to 1). lib/generate_report.ts renders the FitResult into a printable A4-style standalone HTML hiring report with the 360 Hire dark-teal brand, header with logo, candidate/role/date meta strip, fit-pct hero + recommendation pill, per-competency cards with score bars and two-column evidence/gaps, strengths/concerns/risk_flags/next_steps sections. New endpoints: POST /api/admin/applications/[id]/score (admin-only, loads context, runs score_fit, persists fit_score + recommendation + report_html, flips status completed->reviewed, logs application.reviewed), GET /api/admin/applications/[id]/report (returns the saved report_html with Content-Type text/html, logs report.viewed), PATCH /api/admin/applications/[id]/recommendation (admin override of recommendation/status/notes, logs application.recommendation_set). Replaced the Phase 3 placeholder Recommendation card on the admin review page with a new ScoringPanel client component that: shows "Generate fit score" when fit_score is null and transcript exists (disabled with hint otherwise); after scoring, shows a 28pt fit %, a colored recommendation dropdown (overridable), a status dropdown (Reviewed / Hired / Rejected), a review notes textarea, Save decision button, View full report (opens /api/.../report in a new tab), and Regenerate. Saved notes also render in a separate card below. 22 routes total, build clean. v1 limitations: the report is HTML only (no PDF export); structured FitResult is rendered into report_html but NOT stored as JSON for future querying — if the admin overrides the recommendation, the saved HTML still shows the AI's original; could add a score_data JSONB column later. 360_app working tree confirmed clean.

### 2026-05-23 — Phase 3 complete: continuous video recording + admin review screen
Wired continuous video+audio recording into the candidate interview and built an admin video-review page. On InterviewChat mount, the client now calls getUserMedia({video:{width:640,height:480,frameRate:15-24}, audio:true}) and starts a single long-running MediaRecorder (video/webm with vp9+opus preferred, vp8 fallback, 600kbps cap to keep file sizes reasonable for 30-45min sessions), with timeslice=10s so chunks accumulate periodically. A small 96x72 preview with a red "REC" pill renders in the chat header. When the AI emits the completion code x7y8, isComplete flips, the new useEffect stops the video recorder; its onstop handler bundles the chunks into a Blob, POSTs to a new endpoint /api/apply/[token]/video which uploads to Vercel Blob (public, video/webm) and saves applications.video_url, then redirects to /complete. If the candidate denies camera access, a non-blocking warning is shown and the interview proceeds audio-only without recording; redirect still happens via a 1500ms fallback. Removed the old setTimeout redirects (3 places: getFirstMessage, sendMessage, sendTranscribedText) so the video upload race doesn't drop recordings. Admin review at /admin/applications/[id]: link from the candidate row on the job-detail page; shows status / fit / invited / interview duration stats, the recorded video (HTML5 <video controls>, plus open-in-new-tab), the full transcript with role-colored bubbles, the JD card, and the CV (Open CV button + collapsible extracted text). Logs transcript.viewed and video.viewed audit actions on each visit. tsc + next build clean; 19 routes total. v1 limitations: video is uploaded only at completion (if candidate closes tab mid-interview the recording is lost — Phase 3.1 could stream chunks); mobile candidates may struggle with 30+min recordings due to memory; AI voice (SpeechSynthesis) plays through speakers so it bleeds into the room audio unless candidate uses headphones (acceptable since transcript already has full AI text). 360_app working tree confirmed clean.

### 2026-05-23 — Phase 2 complete: candidate /apply/[token] flow (consent + CV + interview)
Built the candidate-facing experience. The flow is gated server-side by privacy consent then CV upload: app/apply/[token]/page.tsx fetches the application + job + candidate + consent + messages, and renders PrivacyGate -> CVUpload -> InterviewChat in order, redirecting to /apply/[token]/complete when status=completed. PrivacyGate is candidate-context EN/ES (hiring assessment language, not 360 framing) and POSTs to /api/apply/[token]/consent which writes a row to privacy_consents with IP + user-agent. CVUpload accepts PDF/DOC/DOCX up to 10MB, drag-and-drop or click, and POSTs to /api/apply/[token]/cv which uploads to Vercel Blob (public ACL) under cv/<applicationId>/<ts>-<safeName>; if BLOB_READ_WRITE_TOKEN is missing the route returns 500 with a clear error. For PDFs, the route ALSO runs a one-shot Anthropic call (claude-haiku-4-5-20251001 + document type, base64) to extract CV text and saves it to applications.cv_text — non-fatal if it fails. InterviewChat is a port of the latest 360_app InterviewChat (with voice I/O: tap-to-record MediaRecorder + browser SpeechSynthesis for AI voice, two pill toggles for input/output voice/text, localStorage persistence per-token) but rebranded teal #0F3D3E, header "Interview for {jobTitle}", AI bubble label "AI", no respondent-role concept since candidates have only one mode. /api/apply/[token]/chat builds a JD-injected system prompt via the new lib/prompts.ts (single buildCandidatePrompt that splices jobTitle, jobDescription, orgLevel, candidateFirstName, cvText, language; EN/ES; CIT framing; 15-question target with closing question; completion code x7y8, moderation code 5j3k mirroring 360_app), persists each turn to messages tied to application_id, and flips application.status from pending->in_progress on first user message and ->completed on detecting x7y8. /api/apply/[token]/transcribe is the live STT route mirroring 360_app's, language-aware via candidate.preferred_language ?? jobs.language. New audit actions logged: application.accessed, consent.accepted, cv.uploaded, interview.started, interview.completed. NOT in Phase 2 (Phase 3): continuous video recording of the session, viewable by admin. Build green, 17 routes total, 360_app working tree confirmed clean.

### 2026-05-23 — Phase 1 complete: admin + jobs CRUD + candidate invite
Wired up admin auth (lib/auth.ts mirroring 360_app pattern, middleware.ts guarding /admin/dashboard|/admin/jobs|/admin/applications, login + logout API routes), the admin login page at /admin (cloned style, 360 Hire branding in dark teal), shared AdminHeader component (logo + Jobs link + Sign out), the dashboard at /admin/dashboard listing jobs with completed/total candidate counts, the New Job form at /admin/jobs/new (title, description, org_level, language, hiring_manager), and the job detail page at /admin/jobs/[id] which shows the JD, a per-job invite-candidate form (first_name, surname1, email, language), and a candidates table with status pill (Invited/In progress/Completed/Reviewed/Hired/Rejected), fit %, and invited date. Invite flow creates a candidate row (find-or-create by email, lowercased), an application row (find-or-create by job_id+candidate_id so re-inviting returns the existing token), and sends a Resend email in EN or ES via lib/email.ts (slim version, no role variants). Email points to /apply/<token> which doesn't exist yet (will be built in Phase 2). New audit actions: job.created, job.updated, job.archived, application.invited. Build clean. Phase 0 scaffold (this entry's parent) was the migration + lib copy + memory scaffold.

### 2026-05-23 — session initialized (Phase 0 scaffold)
Project created at C:\Users\bolve\360_hire as a fork-style sibling of 360 Evaluate (C:\Users\bolve\360_app). Goal: assess EXTERNAL candidates against a job description, using as much of the existing 360 Evaluate code as possible. Domain differences: one candidate per job (no peers/boss/team_member), no min-evaluators gate, no pooled scoring, no employee portal, no company/employee/evaluation hierarchy. Instead: jobs x candidates x applications, with each application owning an interview token, CV blob URL, continuous video blob URL, fit score and recommendation. Decided: (a) SEPARATE Supabase project, (b) continuous video recording for the whole session (admin-viewable later), (c) EN + ES, (d) candidate uploads CV, (e) same memory pattern, (f) brand "360 Hire" with dark-teal accent #0F3D3E, (g) port 3003 locally, (h) HARD CONSTRAINT: do not modify C:\Users\bolve\360_app from this project. Files written this session: package.json (mirrors 360_app deps + adds @vercel/blob), tsconfig.json, next.config.js, .gitignore, .env.example, next-env.d.ts, app/layout.tsx (basic), app/page.tsx (landing placeholder), lib/supabase.ts (copied verbatim), lib/whisper.ts (copied verbatim), lib/audit.ts (adapted with hiring-domain action enum), supabase/migrations/001_init.sql (jobs, candidates, applications, messages, audit_logs, ai_logs, privacy_consents; RLS service-role-only; gen_random_uuid via pgcrypto; updated_at trigger on jobs), MEMORY_PROTOCOL.md (verbatim from 360_app), CONTEXT.md (initial), CLAUDE.md (project conventions). Migration NOT yet applied; npm install NOT yet run. Next: npm install, fill .env.local, apply migration in Supabase, npm run build to verify.
